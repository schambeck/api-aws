version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - mvn clean install

artifacts:
  files:
    - target/api-aws-1.0.0.jar
    - scripts/start-process.sh
    - appspec.yml
  discard-paths: yes
